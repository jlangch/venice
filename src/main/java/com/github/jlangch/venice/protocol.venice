;; Protocol

(def *PROTOCOL* (. :com.github.jlangch.venice.util.Protocol :new))

(def protocol/open (fn [] (do (. *PROTOCOL* :clear) (. *PROTOCOL* :log :INFO "opened"))))

(def protocol/log (fn [lvl msg] (. *PROTOCOL* :log lvl msg)))

(def protocol/to-string (fn [] (str *PROTOCOL*)))
